# ---for demo mode
# The agent's reasoning loop needs an LLM; everything else runs locally.
OPENAI_API_KEY=sk-...

# --- Optional: only needed if you set APP_MODE=live ---
# APP_MODE=live
# SAGEMAKER_ENDPOINT=
# AWS_REGION=us-east-1
# DATABRICKS_HOST=
# DATABRICKS_HTTP_PATH=
# DATABRICKS_TOKEN=
# PINECONE_API_KEY=
